
import { restaurantType } from 'mystery-diner/helpers/restaurant-type';
import { module, test } from 'qunit';

module('Unit | Helper | restaurant type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restaurantType([42]);
  assert.ok(result);
});

